pipeline {
    agent any
    stages {
        stage('npn install') {
            sh 'npm install'
        }
        stage('build') {
            sh 'npm run build'
        }
        stage('Lint') {
            sh 'ng lint'
        }
        stage('console log') {
            echo "deploying.."
        }
    }
}